### Let's go.

```shell
# request for question info
node new-question.js -o test -t "Max Consecutive Ones" -auto
# or skip requesting
node new-question.js -o test -t "Max Consecutive Ones" -n "findMaxConsecutiveOnes"
```